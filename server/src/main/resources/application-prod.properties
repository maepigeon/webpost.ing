# ── Prod profile ──────────────────────────────────────────────────────────────
# Cookies require HTTPS (secure=true, SameSite=None).
app.dev-mode=false

# !! CHANGE THIS: use an absolute path so the upload directory doesn't depend
#    on where the JAR is launched from.
app.upload-dir=/var/www/webposting/uploads
