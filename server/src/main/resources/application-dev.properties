# ── Dev profile ───────────────────────────────────────────────────────────────
# Cookies work over plain HTTP (no 'secure' flag, SameSite=Lax).
app.dev-mode=true

# Uploads stored relative to the server working directory (server/uploads/).
app.upload-dir=uploads
